{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["NODE_ENV", "API_BASE_URL", "VITE_API_URL"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "build/**", "src-tauri/target/**", "workbench-dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "type-check": {
      "dependsOn": ["^build"]
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**"]
    },
    "clean": {
      "cache": false
    },
    "clean:cache": {
      "cache": false
    },
    "desktop:build": {
      "dependsOn": ["^build"],
      "outputs": ["src-tauri/target/release/bundle/**", "workbench-dist/**"]
    },
    "desktop:dev": {
      "cache": false,
      "persistent": true
    }
  }
}
