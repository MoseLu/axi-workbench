# 包说明

最后更新：2026-04-03 16:33:20 +08:00
父级：[AGENTS.md](../../AGENTS.md)
范围：packages/scaffold-registry

## 角色

- 将 `foundation-*` 和 `feature-*` 包组装为统一的能力图。
- 负责预设策略、依赖展开与默认模块选择。

## 本地约束

- 仅依赖 `@axi/scaffold-kit` 与能力包。
- 不要提示用户、安装依赖或写入项目文件。
- 保持模块组装的确定性；运行期决策必须留在 `@axi/scaffold-runtime`。
- 能力包必须能通过本层被替换，且包之间不出现横向耦合。

## 当前重点

- 在以类型化贡献工作取代模块特定的运行期假设时，保持注册表组合的清晰度。
- 保留单一的组装边界，以便未来新增能力包时无需改动 CLI 代码路径。
