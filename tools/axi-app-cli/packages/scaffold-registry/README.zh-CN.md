# @axi/scaffold-registry

## 角色

能力包与预设策略的装配层。

## 允许的工作区依赖

- `@axi/scaffold-kit`
- 所有 `foundation-*` 包
- 所有 `feature-*` 包

## 负责内容

- 注册表组合
- 预设目录
- 依赖展开与默认模块选择

## 不应做

- 与用户交互（提问）
- 执行安装流程
- 直接写文件
