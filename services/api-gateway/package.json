{
  "name": "@epap/api-gateway",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "go run cmd/gateway/main.go",
    "build": "go build -o bin/gateway ./cmd/gateway",
    "test": "go test ./... && bash scripts/dev-run_test.sh"
  }
}
