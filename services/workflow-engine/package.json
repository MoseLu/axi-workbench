{
  "name": "@epap/workflow-engine",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "uv run --with-requirements requirements.txt uvicorn main:app --reload --host 0.0.0.0 --port 8083",
    "build": "python -m compileall -q .",
    "test": "uv run --with-requirements requirements-dev.txt pytest"
  }
}
