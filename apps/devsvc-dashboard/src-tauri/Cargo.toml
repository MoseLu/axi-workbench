[package]
name = "devsvc-dashboard"
version = "0.1.0"
description = "Axi Dashboard desktop host for workspace applications"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2.4.0", features = [] }

[dependencies]
tauri = { version = "2.8.5", features = ["unstable"] }
