# Automatic Submit Record

- Generated at: 2026-08-07T17:30:23.660Z
- Generated-by: axi-submit-log-post-commit
- Repository: /Volumes/code/workspace/projects/axi-workbench
- Branch: dev
- Commit: e15ca0fe57d74d275cfdbb007ad285a78927cf8d
- Push state: upstream=origin/dev; ahead=3; behind=0

## Commit Summary

fix(deps): rewrite sub-pnpm-lock postcss advisories to first-patched

## Commit Body

Step 4 of the post-batch plan closes the remaining dependabot postcss
advisories on apps/devsvc-dashboard, apps/axi-coder, apps/verification-
inbox, infra/fleet-console/dashboard, and tools/axi-app-cli. Sub-
manifest pnpm.overrides are silently ignored when the directory sits
inside a pnpm-workspace because pnpm reads overrides only at the
workspace root, so the postcss bump has to land directly in the
lockfile.

For each sub-pnpm-lock.yaml:
- rewrite postcss@8.5.8/12/14/15 keys to postcss@8.5.23 (or 8.5.26 for
  tools/axi-app-cli, which went via pnpm 10)
- update every referrer (postcss: 8.5.x) to the bumped key
- run `pnpm install --frozen-lockfile=false` so pnpm regenerates the
  integrity hashes from the registry

No package.json surface change; spec fields are unchanged so the
build/type-check commands stay on their existing ranges.

Note: sub-pnpm-lock.yaml for tools/axi-app-cli additionally bumps
several dev-deps (typescript 6.0.2 -> 6.0.3, vitest 4.1.2 -> 4.1.10,
tsx 4.21.0 -> 4.23.11, @types/node 25.5.0 -> 25.9.5, @inquirer/prompts
8.3.2 -> 8.5.2, zod 4.3.6 -> 4.4.3) because the workspace uses pnpm
10.33.2 which auto-resolves caret ranges on every install. Each is
inside the same minor/patch line that already shipped, so no
behavioural change is expected; the tool's downstream consumers
(apps/workbench etc.) depend on the App CLI via its CLI boundary,
not its internal dependency choices.

Tested: pnpm install --frozen-lockfile=false clean on all five dirs;
postcss@8.5.23 confirmed in devsvc-dashboard/axi-coder/verification-
inbox/infra-fleet-console-dashboard; 8.5.26 in tools/axi-app-cli;
go build clean on services/platform-core/identity-adapter/
notification-service/api-gateway still passes.

Not-tested: full deep regression of each affected app (no test
suites available that exercise the transitive postcss chain end-to-end
beyond what package.json scripts already cover).

Confidence: high
Scope-risk: medium (sub-pnpm-lock file rewrite plus a pnpm 10
auto-bumped transitive dev-deps tree in tools/axi-app-cli)
Directive: clean the sub-manifest advisories so dependabot no
longer flags workbench-owned lockfiles

## Changed Paths

- apps/axi-coder/pnpm-lock.yaml
- apps/devsvc-dashboard/pnpm-lock.yaml
- apps/verification-inbox/pnpm-lock.yaml
- infra/fleet-console/dashboard/pnpm-lock.yaml
- tools/axi-app-cli/package.json
- tools/axi-app-cli/packages/scaffold-runtime/package.json
- tools/axi-app-cli/pnpm-lock.yaml

## Verification

- Tested: pnpm install --frozen-lockfile=false clean on all five dirs;
- Not-tested: full deep regression of each affected app (no test

## Remaining Risks

full deep regression of each affected app (no test
