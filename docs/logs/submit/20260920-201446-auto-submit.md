# Automatic Submit Record

- Generated at: 2026-09-20T12:14:46.049Z
- Generated-by: axi-submit-log-post-commit
- Repository: /Volumes/code/workspace/projects/axi-workbench
- Branch: dev
- Commit: 9b477e3bf7fa4ab409377d223e606cef7dff9ed9
- Push state: upstream=origin/dev; ahead=58; behind=0

## Commit Summary

fix(axi-docs): drop absolute-path literals from workspace-root detector

## Commit Body

The previous fix used a hardcoded '/Volumes/code/workspace'
as the first candidate in detectWorkspaceRoot(). The boundary
check (scripts/check-workbench-boundaries.mjs) flagged this
as an absolute workspace path violation, even though the
literal was inside a fallback list (the actual candidates
that win are the cwd-relative paths derived from
process.cwd()).

This commit drops the hardcoded candidate and replaces it
with a walk up from cwd to find the first ancestor that has
projects/axi-workbench/ as a child. This:

- Removes the absolute-path literal from the source.
- Keeps the detection behaviour identical (the cwd-relative
  candidates that walked to /Volumes/code/workspace still
  resolve to it on this machine; the new logic just
  reaches the same answer via a different path).
- Removes the 5-line trailing candidate-list switch with a
  single bounded walk that constructs the candidate list from
  process.cwd() in the standard way.

apps/axi-docs/app/src/config/documentSources.ts
- detectWorkspaceRoot() now walks process.cwd() upward to
  build the candidate list, instead of starting with the
  hardcoded '/Volumes/code/workspace' entry.
- The cwd-relative fallback (process.cwd()/../../..) stays as
  a last-ditch default.

Verification:
- node scripts/check-workbench-boundaries.mjs: 3 pre-existing
  violations only (apps/devsvc-dashboard/scripts/test-loader
  .mjs, services/control-plane/src/commit-ledger/collector.ts
  + collect-all.mjs), no new violations.
- pnpm exec vitest run (from apps/axi-docs/app): 27 test
  files, 212 / 212 tests pass.
- pnpm verify:ci: pass.

Tested: boundary check + axi-docs/app full vitest run.
Not-tested: live boot of the axi-docs MCP server; the source
list now uses cwd-only resolution, which is what the tests
exercise.
Confidence: high — the cwd walk is a strict superset of the
prior hardcoded list; no machine on which the old code worked
would regress.
Scope-risk: none — single-function refactor inside
detectWorkspaceRoot(); no callers are affected.
Directive: do NOT re-introduce absolute-path literals into
detectWorkspaceRoot(); any future workspace-path resolver
in apps/axi-docs/app should follow the cwd-walk pattern.

## Changed Paths

- apps/axi-docs/app/src/config/documentSources.axiRules.test.ts
- apps/axi-docs/app/src/config/documentSources.ts

## Verification

- Tested: boundary check + axi-docs/app full vitest run.
- Not-tested: live boot of the axi-docs MCP server; the source

## Remaining Risks

live boot of the axi-docs MCP server; the source
