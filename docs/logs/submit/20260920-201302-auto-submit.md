# Automatic Submit Record

- Generated at: 2026-09-20T12:13:02.941Z
- Generated-by: axi-submit-log-post-commit
- Repository: /Volumes/code/workspace/projects/axi-workbench
- Branch: dev
- Commit: 20136b132ec37fae7b248752c489e65482fe0256
- Push state: upstream=origin/dev; ahead=56; behind=0

## Commit Summary

fix(axi-docs): make workspace-root path resolver cwd-invariant

## Commit Body

The wip merge into dev exposed two pre-existing test failures
in apps/axi-docs/app that pre-dated the WFB-BASE-MERGE:

1. T2 ('resolves axi-rules path from AXI_RULES_PATH or
   workspace default'): resolveWorkspacePath() used
   `path.resolve(process.cwd(), '../../..', ...)`, which assumed
   apps/axi-docs/app/src/config/ is three levels under the
   workspace root. Once apps/axi-docs moved from
   projects/axi-docs/app/ to projects/axi-workbench/apps/axi-docs/
   app/ (one level deeper under the workbench monorepo), the
   resolution needed four levels, not three.

2. T3 ('keeps the 8 original sources intact'): the registry
   grew from 8 to 10 sources when wip added skill-registry as a
   new source on top of the pre-existing 8; the test still
   asserted length === 9 but the post-wip reality is 10.

This commit fixes both:

apps/axi-docs/app/src/config/documentSources.ts
- Replace the brittle `path.resolve(process.cwd(), '../../..',
  ...)` with a `detectWorkspaceRoot()` helper that walks a
  list of candidate ancestors and picks the first one whose
  `projects/axi-workbench` child exists. This is invariant
  under three cwd values:
    - /Volumes/code/workspace/projects/axi-workbench/apps/axi-docs/app
    - /Volumes/code/workspace/projects/axi-workbench/apps/axi-docs
    - /Volumes/code/workspace/projects/axi-workbench
  all of which resolve to /Volumes/code/workspace.
- Drop the now-stale `fileURLToPath(import.meta.url)` walk
  approach (did not survive vitest's transpile boundaries).
- resolveProjectPath() now anchors at the workspace root +
  projects/, matching the workspace-graph contract.

apps/axi-docs/app/src/config/documentSources.axiRules.test.ts
- T3: length assertion updated from 9 to 10 (the registry
  contains 8 originals + skill-registry + axi-rules).
- T2 unchanged (still asserts /Volumes/code/workspace/projects/
  axi-rules as the workspace-default path).

Verification:
- pnpm exec vitest run (from apps/axi-docs/app): 27 test files,
  212 / 212 tests pass.
- pnpm --filter @axi/workbench exec tsc --noEmit: exit 0.
- pnpm verify:ci: pass.

Tested: full apps/axi-docs/app vitest run; apps/workbench
typecheck + tests; workbench verify:ci.
Not-tested: live boot of the axi-docs MCP server; live HTTP
roundtrip against /Volumes/code/workspace/projects/axi-rules/.
Confidence: medium-high — the cwd-invariant detection is
robust against the three known process.cwd() values; if the
test runs from an unusual cwd (e.g. /tmp/) it falls back
to a 4-level relative walk, which is the worst-case
heuristic.
Scope-risk: low — only apps/axi-docs/app/src/config is touched;
all other apps and services are unaffected.
Directive: any subsequent path-resolver or test-count assertion
in apps/axi-docs/app should follow the same pattern (use
`detectWorkspaceRoot()` and count the actual registry).

## Changed Paths

- apps/axi-docs/app/src/config/documentSources.axiRules.test.ts
- apps/axi-docs/app/src/config/documentSources.ts

## Verification

- Tested: full apps/axi-docs/app vitest run; apps/workbench
- Not-tested: live boot of the axi-docs MCP server; live HTTP

## Remaining Risks

live boot of the axi-docs MCP server; live HTTP
