# Automatic Submit Record

- Generated at: 2026-08-07T16:00:49.832Z
- Generated-by: axi-submit-log-post-commit
- Repository: /Volumes/code/workspace/projects/axi-workbench
- Branch: dev
- Commit: a7d63b0828ea4e1498efa04e0458cea3233eac79
- Push state: upstream=origin/dev; ahead=5; behind=0

## Commit Summary

fix(deps): add per-package pnpm.overrides for transitive advisories

## Commit Body

Continue the dependabot clean-up from commit da977fd: add the same
transitive security pnpm.overrides block to every workbench-owned
manifest whose dependabot alert surfaces under apps/<x>/pnpm-lock.yaml
or infra/fleet-console/dashboard. These overrides collapse the next
pnpm install in each subtree to the patched transitive versions:

- apps/devsvc-dashboard/pnpm-lock.yaml  (#552, #555, #557, #558,
  #572, #574, #575)
- apps/axi-coder/pnpm-lock.yaml          (#552, #555)
- apps/verification-inbox/pnpm-lock.yaml (#552, #554)
- infra/fleet-console/dashboard/pnpm-lock.yaml (#566)

Overridden packages (same as the root): brace-expansion 1.1.18,
js-yaml 3.15.1 / 4.3.1, postcss 8.5.23.

apps/axi-coder already had a sub-overrides block (rollup wasm-node
swap), so we merge into its existing pnpm.overrides; the other
three manifest files did not yet have a pnpm block, so a fresh one
is added.

These overrides are listed in the manifest but the per-package
pnpm-lock.yaml files are intentionally not regenerated by this
commit. The dependabot alerts track the manifest_path (i.e. the
lockfile location), and dependabot also re-evaluates the spec
side; once the lockfile regenerates on the next install (during
the follow-up rebuild batch) the patched transitive versions will
land.

apps/app-search-system/frontend and tools/axi-app-cli are deferred
for separate handling:

- apps/app-search-system/frontend belongs to the axi-docs product
  surface and is out of scope for workbench-owned manifests.
- tools/axi-app-cli is an independent sub-monorepo (see
  tools/axi-app-cli/AGENTS.md) governed by its own lockfile; the
  postcss refresh there is recorded in the file plan only.

Tested: pnpm install in the workbench root (locked and clean);
sub-overrides validate as JSON; existing apps/axi-coder rollup
overrides preserved.
Not-tested: sub-pnpm-lock.yaml regeneration (deferred)
Confidence: high
Scope-risk: narrow (overrides + spec field)
Directive: complete the dependabot clean-up across workbench-owned
manifests so each app can re-install with patched transitives

## Changed Paths

- apps/axi-coder/package.json
- apps/devsvc-dashboard/package.json
- apps/verification-inbox/package.json
- infra/fleet-console/dashboard/package.json

## Verification

- Tested: pnpm install in the workbench root (locked and clean);
- Not-tested: sub-pnpm-lock.yaml regeneration (deferred)

## Remaining Risks

sub-pnpm-lock.yaml regeneration (deferred)
