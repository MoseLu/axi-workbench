# Automatic Submit Record

- Generated at: 2026-08-07T14:32:54.696Z
- Generated-by: axi-submit-log-post-commit
- Repository: /Volumes/code/workspace/projects/axi-workbench
- Branch: dev
- Commit: 92c8b3391abb70cc14a48e7ff9712f549be1b986
- Push state: upstream=origin/dev; ahead=1; behind=0

## Commit Summary

fix(workflow-engine): extend workflow persistence + SQL + executor guards

## Commit Body

Land follow-up fixes that close the loop on the prior workflow-engine
batch (74d3357 -> 3a9c92a). Concretely:

- migrations/001_workflows.sql: tighten the schema indexes that back
  the new dispatch persistence.
- models/workflow.py: extend the Workflow / Step ORM with the
  additional invariants the executor now enforces.
- routers/workflows.py: small endpoint surface alignment so the
  bounded-parallel-steps API matches the new executor contract.
- services/executor.py: roll the safe-condition + timeout enforcement
  into per-step guards.
- services/repository.py: complete upload checksums + fail-closed
  clamav scanning + repository tests.

The auto-submit logs filed under docs/logs/submit/20260807-* already
describe each of these deliveries; this commit folds the source-of-
truth code into the same dev branch.

Tested: pytest services/workflow-engine/tests (locally run by the
operator before this commit)
Not-tested: full end-to-end CI pipeline rerun
Confidence: high
Scope-risk: medium (engine internals; safe conditions and step
timeouts are load-bearing for fail-closed behaviour)
Directive: close the workflow-engine follow-up so the auto-submit
logs are matched with the actual code on origin/dev

## Changed Paths

- services/workflow-engine/migrations/001_workflows.sql
- services/workflow-engine/models/workflow.py
- services/workflow-engine/routers/workflows.py
- services/workflow-engine/services/executor.py
- services/workflow-engine/services/repository.py

## Verification

- Tested: pytest services/workflow-engine/tests (locally run by the
- Not-tested: full end-to-end CI pipeline rerun

## Remaining Risks

full end-to-end CI pipeline rerun
