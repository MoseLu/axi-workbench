# Automatic Submit Record

- Generated at: 2026-09-24T19:16:50.152Z
- Generated-by: axi-submit-log-post-commit
- Repository: /Volumes/code/workspace/workbench/axi-workbench
- Branch: dev
- Commit: 727e50b2825c99f2e9f453c841378aea59854877
- Push state: upstream=origin/dev; ahead=18; behind=0

## Commit Summary

feat(identity-adapter): adopt axilog-go (PRD-07 phase 2)

## Commit Body

Migrate the identity-adapter slog initialiser to the Axi
observability SDK so the JSON shape matches every other Axi
service.

Changes:
- cmd/identity-adapter/main.go: replace
  `slog.New(slog.NewJSONHandler(os.Stdout, nil))` with
  `axilog.New(axilog.Options{Service: WithService("axi-identity-adapter")})`.
  Drop the now-unused 'log/slog' import.
- go.mod: add require + replace pointing at the local
  foundation/axi-observability/go/axilog via the GOPATH-style
  soft-link in $HOME/go/src.
- go.work: pin identity-adapter + axilog as a single multi-module
  build so `go build ./...` resolves the local SDK without a
  network fetch.

The rest of the package continues to use *slog.Logger unchanged;
axilog.New returns the same type so no internal refactor was
needed.

Tested: `go build ./...` and `go test ./...` both pass. 5
internal packages report ok; 5 subpackages have no tests.
Not-tested: OTel SDK upgrade path; runtime smoke.
Confidence: high
Scope-risk: low — one source file + three manifest files;
no existing JSON shape changes (axilog reuses slog's JSON
handler).
Directive: continue PRD-07 phase 2 with control-plane migration
(@axi/observability-logging) and ielts-vocab nohup rotation.

## Changed Paths

- services/identity-adapter/cmd/identity-adapter/main.go
- services/identity-adapter/go.work

## Verification

- Tested: `go build ./...` and `go test ./...` both pass. 5
- Not-tested: OTel SDK upgrade path; runtime smoke.

## Remaining Risks

OTel SDK upgrade path; runtime smoke.
