# Automatic Submit Record

- Generated at: 2026-09-20T02:08:49.683Z
- Generated-by: axi-submit-log-post-commit
- Repository: /Volumes/code/workspace/projects/axi-workbench
- Branch: chore/merge-baseline-projects-2026-09-20
- Commit: edb4998232dc8a5d3fdf1caa942a8d9f0c9a53b8
- Push state: no upstream

## Commit Summary

chore(apps): drop apps/axi-docs/app inner pnpm-workspace.yaml + lockfile

## Commit Body

axi-docs was originally a standalone monorepo with its own
pnpm-workspace.yaml, pnpm-lock.yaml, and pnpm.overrides block
in apps/axi-docs/app/package.json. After merging into
axi-workbench (commit 00ce02bf), those artifacts became
carried-over noise: the workbench root pnpm-workspace.yaml
already globs apps/* and resolves apps/axi-docs/app as a leaf
member, and the workbench root pnpm.overrides is the authority
for the tree.

Leaving the inner files in place means a developer who runs
`pnpm install` inside apps/axi-docs/app/ will get a different
tree than the root install — the inner overrides pin postcss to
8.5.18 (root: 8.5.23), js-yaml to 3.15.2 / 4.3.2 (root: 3.15.1 /
4.3.1), and pull a parallel hono 4.12.x plus 19 other pinned
versions. That is a latent source of install-graph divergence
that the merger must not inherit.

apps/axi-docs/app/{pnpm-workspace.yaml,pnpm-lock.yaml}
- removed. The app is now a leaf workspace member of the
  workbench root, and its pnpm.overrides block in
  apps/axi-docs/app/package.json still pins the same packages
  locally (workbench honors the nested overrides at the leaf
  package level).
- The inner pnpm.overrides is left in place for now because
  removing it requires a separate pass to confirm none of the
  inner pins are load-bearing for the apps/axi-docs/app/
  toolchain (hono, body-parser, fast-uri, undici, etc.). Tracked
  as a future chore.

Verification: pnpm install at the workbench root no longer
matches apps/axi-docs/app/ as a sibling workspace root; it
sees the app only as a leaf member of apps/* and resolves its
dependencies through the workbench root lockfile. No code
changes.

Tested: pnpm install still succeeds from the workbench root;
apps/axi-docs/app is still resolved as a leaf member (pnpm ls
-l --depth=-1).
Not-tested: a developer who ignores this commit and runs
`pnpm install` from inside apps/axi-docs/app/ — that path
will re-create the inner lockfile, which is acceptable as
long as they then re-delete it.
Confidence: high — the deletion is mechanical and pnpm's own
behaviour confirms apps/axi-docs/app is now treated as a leaf.
Scope-risk: low — the inner lockfile was already superseded by
the workbench root lockfile; nothing in apps/axi-docs/app
runtime depends on a per-app install root.
Directive: delete the inner pnpm.overrides block in
apps/axi-docs/app/package.json in a follow-up chore only after
verifying the inner pins are not load-bearing for the
hono / fast-uri / undici stack used by apps/axi-docs/app
MCP server.

## Changed Paths

- apps/axi-docs/app/pnpm-lock.yaml
- apps/axi-docs/app/pnpm-workspace.yaml

## Verification

- Tested: pnpm install still succeeds from the workbench root;
- Not-tested: a developer who ignores this commit and runs

## Remaining Risks

a developer who ignores this commit and runs
