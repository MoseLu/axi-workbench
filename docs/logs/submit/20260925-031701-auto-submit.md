# Automatic Submit Record

- Generated at: 2026-09-24T19:17:01.800Z
- Generated-by: axi-submit-log-post-commit
- Repository: /Volumes/code/workspace/workbench/axi-workbench
- Branch: dev
- Commit: 7cf07adb8202b4c53da7e94bb0e348677dd0a163
- Push state: upstream=origin/dev; ahead=19; behind=0

## Commit Summary

feat(control-plane): adopt @axi/observability-logging (PRD-07 phase 2)

## Commit Body

Migrate diagnostic chatter in src/server.mjs from bare
console.* to structured JSON via the Axi observability Node SDK.
The SDK is imported as an optional dependency: when present
the records are emitted as one JSON line per write; when absent
the fallback to console.* keeps the install working.

Changes:
- New _logInfo / _logWarn / _logError helpers route through the
  SDK when available, otherwise call console.* with the same
  semantic level.
- 5 console.* call sites migrated:
  - commit-ledger: persistence store loaded / failed to load
  - fs-watcher stop (×2) / failed to start
  - commit-ledger fs_watcher disabled
  - commit-ledger fs_watcher enabled

Kept as console.log:
  - 'control-plane listening on http://...:port' (line 1092)
    because downstream scripts pipe it through jq.
- 'console.log(JSON.stringify(...))' in smoke.mjs and
  'console.warn' inside commit-ledger/persistence.mjs: kept
  as console.* because they are protocol output / internal
  module; full SDK adoption in those files would require
  additional refactoring.

Tested: node --check src/server.mjs (syntax); no test suite
runs because control-plane's test directory is generated by a
later batch.
Not-tested: full server runtime with the SDK installed (the
pnpm workspace symlink is in place but the SDK dist/ is rebuilt
on the consuming side).
Confidence: high
Scope-risk: low — one file; no JSON shape change (only the
diagnostic records).
Directive: continue PRD-07 phase 2 with the ielts-vocab
start-microservices.sh rotation, then update progress memory.

## Changed Paths

- services/control-plane/src/server.mjs

## Verification

- Tested: node --check src/server.mjs (syntax); no test suite
- Not-tested: full server runtime with the SDK installed (the

## Remaining Risks

full server runtime with the SDK installed (the
