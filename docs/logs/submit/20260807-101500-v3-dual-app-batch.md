# Submit Record — Axi Workbench v3 Dual-App Batch (2026-08-07)

- Generated at: 2026-08-07T01:36:00.000Z
- Generated-by: git-commit-batch whole-repository diff cleanup
- Repository: /Volumes/code/workspace/projects/axi-workbench
- Branch: dev
- Push state: ahead=6; not pushed yet (roll-up first)

## Batch Scope

Convert the session-time diff into 6 feature-aligned local commits
plus this roll-up. The session began on top of `b742127` (the prior
14-commit v2 single-SPA batch landed earlier today) and lifted the
project to v3 (two independent user applications: Web admin
`apps/workbench` + mobile `apps/workbench-mobile`).

## Commit Roll-up

1. `b742127` (parent) — docs(log): record v2 workbench batch roll-up submit log
2. new — feat(mobile): add apps/workbench-mobile and shared foundation layer
3. new — fix(workbench): align Login page with shared foundation auth contract
4. new — docs(state): refresh PRD/TDD/TODO/MILESTONE/CHANGELOG/VERIFICATION to v3 dual-app
5. new — docs(root): refresh AGENTS/README/INDEX/HANDOFF for v3 dual-app workbench
6. new — chore(stubs): add root PRD/TDD/TODO/MILESTONE/CHANGELOG/INDEX builder-friendly stubs
7. new — chore(deps): refresh pnpm-lock for v3 dual-app workbench
8. new — docs(log): record v3 dual-app batch roll-up submit log (this file)

## Cross-Cutting Intent

- **Two-application workbench**: `apps/workbench` (Web admin :5173)
  and `apps/workbench-mobile` (mobile app :5174) are independent
  Vite applications. They share only `packages/workbench-foundation`
  (auth + locale) and the broader `@axi/workstation-*` contract
  packages.
- **Document suite v3**: PRD lifts to 14 `REQ-*` rows; TDD adds a
  Mobile application UI test surface and a shared-app-foundation
  row; TODO gains P0/P1 mobile gates; MILESTONE rewrites the
  current-status block; CHANGELOG records the v3 entry; VERIFICATION
  captures both :5173 (1440px) and :5174 (390px) shell evidence.
- **Root + subtree docs**: AGENTS/README/INDEX/HANDOFF refresh so
  the v3 read order calls out both apps and the foundation package
  as the only shared runtime.
- **Builder-friendly stubs**: thin root-level stubs of PRD/TDD/TODO/
  MILESTONE/CHANGELOG/INDEX.md point at docs/state/* as the canonical
  source so `axi-docs/build-projects-index.mjs` can refresh the
  dossier mirror via `project.path + piece`.
- **Lockfile refresh**: pnpm-lock follows the new package surface.

## Out-of-Scope (intentionally left dirty)

- `.workbuddy/` 与 `.superpowers/`, `docs/superpowers/` 是本地 harness
  状态目录，按本仓库 `AGENTS.md` 「不适用对象」段不进入 commit。

## Companion Work in Other Projects

- `axi-docs` mirror dossiers have been refreshed in lockstep (push
  landed at `MoseLu/axi-docs` `dev` `67c82b2`).

## Verification

- 6 + 1 commits each carry Lore trailers (Tested, Not-tested,
  Confidence, Scope-risk, Directive).
- `pnpm --filter @axi/workbench` type-check / test / build still
  green; mobile side commands are recorded in PRD `REQ-VERIFY-002`
  Acceptance Criteria.
- `docs/state/PRD.md` 与 `docs/state/TDD.md` 在每次 commit 中都被
  引用；`docs/state/TODO.md` 中每个 `REQ-*` 都对应一个 test。

## Next-Safe-Action

将这一组 6 commits 与本 roll-up 一次性推送到 `origin/dev`（last
verified 2026-08-07 已包括在 v2 batch 内，v3 batch 待 push）。
