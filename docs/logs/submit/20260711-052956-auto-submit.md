# Automatic Submit Record

- Generated at: 2026-07-10T21:29:56.215Z
- Generated-by: axi-submit-log-post-commit
- Repository: /Volumes/code/workspace/projects/axi-workbench
- Branch: dev
- Commit: 8592abecf9c88c88d270fefed15f1186e7951e2a
- Push state: upstream=origin/dev; ahead=9; behind=0

## Commit Summary

docs(control-plane): OpenAPI 3.0 spec for mobile v1 + schema conformance test (DevHub stage-B item 8)

## Commit Body

Adds the machine-readable contract for the Android client so it can generate / validate DTOs without hand-coding against server.mjs. Per the stage-B plan §D decision the spec is hand-written and validated by a small in-tree test, not by a third-party OpenAPI validator: this keeps the control-plane package at 0 npm dependencies.

services/control-plane/openapi/mobile.v1.yaml (new, ~470 lines):
- openapi: 3.0.3, info.title 'Axi Workstation Control Plane — Mobile v1'.
- 10 documented paths covering pair/start, pair/confirm, auth/token, auth/nonce, pair/revoke, workspace, projects/{id}, jobs, jobs/{id}/cancel, approvals/{id}/decision.
- Each operation has at least one response and references a JSON schema in components/schemas. MobileActionRequest enforces the six-field contract (idempotencyKey + projectId + actionType; deviceId flows from the bearer token; approvalRef is conditional on the approvals-decision endpoint).
- Components/schemas include: PairStartRequest, PairStartResponse, PairConfirmRequest, PairConfirmResponse, TokenRequest, TokenResponse, NonceResponse, RevokeRequest, RevokeResponse, MobileActionRequest, MobileApprovalDecision, MobileJobAccepted, MobileJobPendingApproval, MobileJobCancelResponse, MobileApprovalDecisionResponse, MobileProjectCard, MobileWorkspaceSnapshot, MobilePreview, MobileAction, ApprovalSummary, JobSummary, MobileActionError.
- MobilePreview.mode enum is restricted to {none, embedded_web, external_web} — internal_web / iframe sandbox etc. are explicitly absent.
- Security scheme: bearerAuth = http + bearer + HS256 with the 'TTL 1h' hint. Static AXI_MOBILE_OWNER_TOKEN fallback is honoured only when AXI_MOBILE_PAIRING_ENABLED is not 'true' (already documented in service comments).
- Standard responses: BadRequest, Unauthorized, NotFound, PairingDisabled.

services/control-plane/test/mobile-openapi-schema.test.mjs (new, 12 cases):
- openapi 3.0.x declared; info.title set; every documented path / verb appears; every $ref resolves into components/{schemas,responses}; the 22 expected schemas exist; MobileActionRequest enforces required + closed shape + idempotencyKey 8-128 chars + pattern ^[A-Za-z0-9_-]+$; MobilePreview.mode enum disallows internal_web; bearerAuth documents HS256 + 1h TTL; PairStartResponse code pattern is ^[0-9]{6}$; idempotency key shape is consistent between request and decision schemas (the latter composes the former via allOf); the info.description points at packages/schemas/src/entities/control-plane.ts as the canonical source so future schema changes keep the two in sync.
- The test deliberately avoids a third-party OpenAPI validator; it reads the YAML text and runs pattern assertions tuned to the shape we maintain. When the spec is reorganised, update both files in the same commit.

Net: 52 -> 64 cases; all pass. npm dep count unchanged.

Tested: pnpm --filter @axi/workstation-control-plane test -> 64/64 (was 52, +12). OpenAPI human-review: yaml parses, all $ref close over, no orphan schemas, MobilePreview enum is safe.

Not-tested: end-to-end consumption by the Android client (deliberately out of scope for this commit; the stage-C Android work will wire an OpenAPI generator against this file).

Confidence: high.
Scope-risk: narrow — only services/control-plane/{openapi,test}; no source or runtime change.
Directive: After this commit, the machine-readable contract for /mobile/v1/* is in the repo. Any future change to the six-field contract, the gate response shape, or the preview enum MUST be mirrored into this YAML + the schema conformance test in the same commit. The Android client (stage-C) can now run openapi-generator-cli against this file without pulling a runtime dependency into this package.

## Changed Paths

- services/control-plane/openapi/mobile.v1.yaml
- services/control-plane/test/mobile-openapi-schema.test.mjs

## Verification

- Tested: pnpm --filter @axi/workstation-control-plane test -> 64/64 (was 52, +12). OpenAPI human-review: yaml parses, all $ref close over, no orphan schemas, MobilePreview enum is safe.
- Not-tested: end-to-end consumption by the Android client (deliberately out of scope for this commit; the stage-C Android work will wire an OpenAPI generator against this file).

## Remaining Risks

end-to-end consumption by the Android client (deliberately out of scope for this commit; the stage-C Android work will wire an OpenAPI generator against this file).
