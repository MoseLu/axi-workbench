# Automatic Submit Record

- Generated at: 2026-09-20T11:38:09.652Z
- Generated-by: axi-submit-log-post-commit
- Repository: /Volumes/code/workspace/projects/axi-workbench
- Branch: chore/merge-baseline-projects-2026-09-20
- Commit: 9bab6c9f844cda484b454189811ad178c4273517
- Push state: no upstream

## Commit Summary

test(workbench): WFB-ROUTER-001 migrate react-router 7 SSR tests to data-router

## Commit Body

react-router 7 (workbench ^7.18) moved the location context
into RouterProvider. The pre-v7 <StaticRouter> / <MemoryRouter>
shape leaves <Link>'s LinkWithRef useContext() returning null,
which surfaced as 'Cannot destructure property basename of
React.useContext(...) as it is null' when renderToStaticMarkup
or @testing-library/react rendered any tree containing <Link>.

The two test files blocked by this were gated through
describe.skip in commit 008f0108 with a TODO annotation. This
commit resolves WFB-ROUTER-001 by switching the production
renderer and the test consumer to the v7 data-router API.

apps/workbench/src/legal-ssr-entry.tsx
- renderLegalDocument() now uses createMemoryRouter +
  RouterProvider instead of <StaticRouter>. The /legal/{kind}
  route definition replaces the legacy <StaticRouter location>
  prop, and RouterProvider supplies the v7 location context that
  <Link> + useNavigate() consumers expect. Public API
  (renderLegalDocument(kind) -> string) is unchanged.

apps/workbench/src/legal-ssr-entry.test.tsx
- restore the 'renders the legal document in a router' case
  under a new describe('... (react-router 7 data router)') block.
  The previous case name explicitly omitted I18nProvider; the
  restored version keeps that intent (LegalDocument renders
  zh-CN copy from a JSON import, no i18n provider required).
- the four pre-existing non-router assertions (renders terms /
  privacy without I18nProvider, no SPA bundle, in-page nav
  selector) remain unchanged.

apps/workbench/src/pages/admin/me/DesktopSettingsPage.test.tsx
- the single test that was describe.skip-ed in commit 008f0108
  is restored and re-shaped for the v7 data router. The route
  definition matches what the page actually serves
  (/admin/me/:key with activeKey=/admin/me/theme) so
  useNavigate() consumers resolve their context.

Result:
- pnpm --filter @axi/workbench test: 41 / 41 files pass,
  198 / 198 tests pass (was 39 passed / 2 skipped before this
  commit).
- pnpm verify:ci: passes.
- workspace-project handoff-check axi-workbench: score 10 /
  errors 0 / warnings 0.

Tested: full apps/workbench vitest run; pnpm verify:ci;
workspace-project handoff-check.
Not-tested: live <Link> navigation in a real browser against
the dev server; that integration path is unchanged from the
pre-v7 workbench and is governed by the same hand-tested
acceptance flows.
Confidence: high — react-router 7's data router is the
documented migration target for any v6 -> v7 user; the
createMemoryRouter + RouterProvider pair matches the pattern
shipped in the v7 docs and is what react-router-dom's own
internal tests use.
Scope-risk: low — only apps/workbench/src/legal-ssr-entry.tsx +
two test files change. The renderLegalDocument public API is
unchanged; if any external tool (e.g. apps/devsvc-dashboard's
SSR fallback or a future SSR-time legal-document render) was
importing renderLegalDocument and expecting v6 output, the
output string is unchanged because the v7 data router
renders the same LegalDocument subtree.
Directive: this commit resolves the only outstanding test skip
block from the baseline-merge chore chain; no follow-up
WFB-ROUTER-001 work remains in apps/workbench. Future router
work (e.g. switching the runtime app shell from React Router
6 patterns to v7 data router) is out of scope for this
follow-up.

## Changed Paths

- apps/workbench/src/legal-ssr-entry.test.tsx
- apps/workbench/src/legal-ssr-entry.tsx
- apps/workbench/src/pages/admin/me/DesktopSettingsPage.test.tsx

## Verification

- Tested: full apps/workbench vitest run; pnpm verify:ci;
- Not-tested: live <Link> navigation in a real browser against

## Remaining Risks

live <Link> navigation in a real browser against
