# Automatic Submit Record

- Generated at: 2026-09-20T03:49:22.437Z
- Generated-by: axi-submit-log-post-commit
- Repository: /Volumes/code/workspace/projects/axi-workbench
- Branch: chore/merge-baseline-projects-2026-09-20
- Commit: 2a11d9d52de294347a5d8fab5c4eda6662111aae
- Push state: no upstream

## Commit Summary

chore(packages): drop unused @epap/schemas-compat placeholder

## Commit Body

packages/epap-schemas-compat was a 1-file legacy compat shim
that re-exported '@axi/workstation-contracts' under the historical
@epap package name. It had:

- src/index.ts: a single `export * from '@axi/workstation-contracts'`
- package.json: name was already '@axi/schemas' (a leftover from
  the rename); the directory name still carried the old @epap brand.
- zero workbench importers (grep across apps/, packages/, scripts/,
  AGENTS.md, Makefile, README.* finds no usage)
- zero workspace.graph consumers
- zero pnpm-lock consumers

After the workbench-foundation rename and the schemas package
landing, the shim has been a dead placeholder since at least
2026-09. Dropping it removes a confusing legacy directory name
without removing any real code; consumers that still want the
old import path can use '@axi/workstation-contracts' directly.

Removed:
- packages/epap-schemas-compat/package.json
- packages/epap-schemas-compat/src/index.ts

pnpm install still passes (the directory was never picked up by
the workbench pnpm-workspace.yaml 'packages/*' glob because its
own package.json declared itself '@axi/schemas', but there were
zero importers so the lockfile had no consumer entry). pnpm
verify:ci still passes.

Tested: pnpm install exits 0; pnpm verify:ci exits 0.
Not-tested: workspace-project validate (out of scope; the shim
was not registered as an admitted project).
Confidence: high — grep + lockfile inspection both show zero
importers; the shim's own export-target package
('@axi/workstation-contracts') still re-exports the underlying
contracts intact under packages/workstation-foundation's src.
Scope-risk: none — no other package depends on the directory
or on @epap/schemas; dropping it cannot regress any consumer.
Directive: if any external (out-of-workspace) consumer was
relying on the @epap/schemas import path, they need to switch
to '@axi/workstation-contracts' (or to '@axi/schemas' via
packages/schemas/src/index.ts which already exports the
contract types).

## Changed Paths

- packages/epap-schemas-compat/package.json
- packages/epap-schemas-compat/src/index.ts

## Verification

- Tested: pnpm install exits 0; pnpm verify:ci exits 0.
- Not-tested: workspace-project validate (out of scope; the shim

## Remaining Risks

workspace-project validate (out of scope; the shim
