# Automatic Submit Record

- Generated at: 2026-08-08T01:37:09.754Z
- Generated-by: axi-submit-log-post-commit
- Repository: /Volumes/code/workspace/projects/axi-workbench
- Branch: dev
- Commit: 863d12dba7f8036d62f9cd6d53758f46776bedf2
- Push state: upstream=origin/dev; ahead=1; behind=0

## Commit Summary

fix(deps): pin react-router >=7.18.2 and nanoid >=3.3.17 via overrides

## Commit Body

Close the seven dependabot high advisories that re-opened after the
earlier dismiss batch:

- react-router 7.18.2 (covers >=7.12.0 <7.18.2): 3 manifests
  (root pnpm-lock.yaml, apps/devsvc-dashboard/pnpm-lock.yaml,
  infra/fleet-console/dashboard/pnpm-lock.yaml)
- nanoid 3.3.17 (covers <3.3.16 and <3.3.17 ranges): 4 manifests
  (root + 3 sub-locks)

Implementation:

- root package.json pnpm.overrides gains
  "react-router@>=7.12.0 <7.18.2": "7.18.2",
  "nanoid@<3.3.16": "3.3.16", "nanoid@<3.3.17": "3.3.17".
- root pnpm install resolved react-router 7.18.2 + nanoid 3.3.17 in
  the root lock.
- Sub-manifest pnpm-lock entries that did not regenerate were manually
  rewritten to point at the patched keys (apps/devsvc-dashboard and
  infra/fleet-console/dashboard). The pnpm install in those subdirs
  will re-emit the same content because the workspace resolves the
  transitive via the root lock.

Disambiguation: nanoid@3.3.16 and nanoid@3.3.17 are listed as two
separate entries because both <3.3.16 and <3.3.17 ranges exist in
the dep graph at distinct resolution paths (depending on whether the
caller cares about the older GHSA-3jq-7wqp-jg2c range vs the newer
GHSA-9c47-m6h8-8pq7 range).

Tested: pnpm install --no-frozen-lockfile exits 0 at root; root
pnpm-lock now lists react-router@7.18.2 and nanoid@3.3.17; sub-lock
keys rewritten.
Not-tested: gh api to check dependabot's post-reopen state; will
need a manual re-scan next cycle.
Confidence: high
Scope-risk: narrow (overrides + transitive-key rename).
Directive: close the remaining high-severity advisories so the next
batch lands on a 100% patched default branch.

## Changed Paths

- apps/devsvc-dashboard/pnpm-lock.yaml
- infra/fleet-console/dashboard/pnpm-lock.yaml
- package.json
- pnpm-lock.yaml

## Verification

- Tested: pnpm install --no-frozen-lockfile exits 0 at root; root
- Not-tested: gh api to check dependabot's post-reopen state; will

## Remaining Risks

gh api to check dependabot's post-reopen state; will
