# Automatic Submit Record

- Generated at: 2026-08-09T20:33:47.857Z
- Generated-by: axi-submit-log-post-commit
- Repository: /Volumes/code/workspace/projects/axi-workbench
- Branch: dev
- Commit: 866abad0f1c1563476bfec7ddf5caa3150ba697d
- Push state: upstream=origin/dev; ahead=61; behind=0

## Commit Summary

fix(workbench): render sidebar, breadcrumbs, search and chrome via i18n

## Commit Body

MainLayout.tsx and Sidebar.tsx still rendered literal Chinese labels for
the desktop navigation, breadcrumb chain, global search palette, avatar
menu, topbar actions, floating tool dock and sidebar search box, even
though every required key now lives in the locale dictionaries after the
previous data-layer commit.

Thread every literal through useI18n():
- Sidebar menuData becomes a per-render array so t() resolves at render
  time.
- MainLayout switches its desktop nav source to
  workbenchDesktopNavGroupsWithKeys and rebuilds the visible groups with
  localized labels (filter still operates on the rendered string).
- Breadcrumb rendering uses item.labelKey when present, falling back to
  the canonical label otherwise.
- Global search palette localizes group / label / description for both
  nav and corpus entries; subtitleKey / titleKey drive the corpus hits.
- Avatar menu, topbar settings action, breadcrumb label, sidebar search
  placeholder and global search shortcut label all go through t().
- resolveMenuRoute returns labelKey so the tab bar shows the same string
  as the sidebar; the project detail and handoff prefix entries share
  the new nav.projectDetail and existing nav.handoff keys.
- shellPlugins localizes notification / locale items through t().
- Floating tool dock uses the new layout.floatingTools.* namespace.

Add the missing keys to zh-CN.json and en-US.json.

Tested:
- pnpm --filter @axi/workbench type-check
- pnpm --filter @axi/workbench test (19 files, 64 tests)
- pnpm --filter @axi/workbench build

Not-tested:
- Full cross-browser matrix, Android packaging, and production deployment

Confidence:
- High for both locales at the chrome level; page-internal literals
  remain out of scope for this commit.

Scope-risk:
- MainLayout.tsx, Sidebar.tsx, two locale dictionaries only.

Directive:
- All visible chrome strings must reach the renderer through t(); do
  not reintroduce literal Chinese labels in MainLayout or Sidebar even
  for zh-CN users.

## Changed Paths

- apps/workbench/src/components/Layout/Sidebar.tsx
- apps/workbench/src/i18n/locales/en-US.json
- apps/workbench/src/i18n/locales/zh-CN.json
- apps/workbench/src/layouts/MainLayout.tsx

## Verification

- Tested: not recorded
- Not-tested: none

## Remaining Risks

none
