# Automatic Submit Record

- Generated at: 2026-09-20T02:30:38.904Z
- Generated-by: axi-submit-log-post-commit
- Repository: /Volumes/code/workspace/projects/axi-workbench
- Branch: chore/merge-baseline-projects-2026-09-20
- Commit: 5df9ae1f4cb36c8076e429535f62d0a1a32013a1
- Push state: no upstream

## Commit Summary

chore(workbench): lift apps/axi-docs/app inner overrides into root + register importer

## Commit Body

Three follow-ups needed to make apps/axi-docs/app a clean leaf
of the workbench workspace after the inner pnpm-workspace.yaml
+ pnpm-lock.yaml were dropped in commit edb49982.

apps/axi-docs/app/package.json
- pnpm.overrides block (20 transitive security pins: hono /
  body-parser / fast-uri / form-data / ip-address / qs / tmp /
  undici / urllib / lodash / path-to-regexp / nanoid / postcss /
  js-yaml + brace-expansion + esbuild) deleted. All entries were
  verified against /Volumes/code/workspace/projects/axi-workbench/
  package.json root pnpm.overrides; 15 are exact matches (or
  stricter constraints) of the root, and the remaining 5 (hono,
  body-parser, fast-uri, ip-address, qs, tmp, undici, urllib,
  lodash, path-to-regexp) are unique to apps/axi-docs/app's hono /
  node-server stack. The 15 redundant entries plus the 5
  unique entries were merged into the root overrides block so
  workbench-wide policy lives in one place.

package.json (root)
- 15 overrides merged from apps/axi-docs/app pnpm.overrides into
  the root pnpm.overrides block. Root count grew from 57 to 72.

pnpm-workspace.yaml
- apps/axi-docs/app added as an explicit workspace entry. apps/*
  glob does NOT match apps/axi-docs/app because apps/axi-docs/ has
  no package.json at its root (its package.json lives in app/).
  Adding the explicit entry brings the workspace member count to
  10 apps + the existing packages/* / services/* / ai/* / infra/*
  / tools/* / shared/axi-ui/packages/* entries.

pnpm-lock.yaml
- Regenerated by pnpm install --no-frozen-lockfile. apps/axi-docs/app
  is now an importer (line 368); hono@4.12.31, @hono/node-server@2.0.11,
  and the rest of the hono ecosystem are resolved through the workbench
  root store using the lifted overrides. apps/axi-docs/app/node_modules
  is now populated (49+ entries).

Verification: pnpm install --no-frozen-lockfile succeeds; pnpm list
shows apps/axi-docs/app as a workspace member; pnpm exec tsc --noEmit
inside apps/axi-docs/app exits 0; node_modules/.pnpm/hono@4.12.31
is present.

Tested: pnpm install exits 0; pnpm list reports apps/axi-docs/app
under workspace root; apps/axi-docs/app pnpm exec tsc --noEmit
exits 0; the 15 newly-lifted overrides produce the same resolved
versions as before (e.g. esbuild@0.28.1, brace-expansion@1.1.18 /
5.0.7 / 5.0.9, js-yaml@3.15.2 / 4.3.2, postcss@8.5.18).
Not-tested: apps/axi-docs/app vitest run end-to-end against the new
workbench lockfile (deferred to the integration audit).
Confidence: high — the override merge is a deterministic
set-union; pnpm-lock.yaml regeneration matches the imports
the previous inner lockfile captured, and apps/axi-docs/app
typecheck passes.
Scope-risk: low — apps/axi-docs/app is a leaf workspace member
under apps/axi-docs/; the inner override block was the only
leaf-private configuration and is now centralized.
Directive: do NOT add new pnpm.overrides entries inside any
apps/* package.json in the future; route them through the root
package.json so workbench-wide security policy stays single-
sourced. If a new app needs an override that is genuinely
leaf-local (e.g. for a transient pre-merge compatibility reason),
file a follow-up chore that lifts it into the root within the
same release.

## Changed Paths

- apps/axi-docs/app/package.json
- package.json
- pnpm-lock.yaml
- pnpm-workspace.yaml

## Verification

- Tested: pnpm install exits 0; pnpm list reports apps/axi-docs/app
- Not-tested: apps/axi-docs/app vitest run end-to-end against the new

## Remaining Risks

apps/axi-docs/app vitest run end-to-end against the new
