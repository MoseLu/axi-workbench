# Automatic Submit Record

- Generated at: 2026-07-10T15:53:38.851Z
- Generated-by: axi-submit-log-post-commit
- Repository: /Volumes/code/workspace/projects/axi-workbench
- Branch: dev
- Commit: a7ec2849599a83294cc45c0303a16f856c461474
- Push state: upstream=origin/dev; ahead=3; behind=0

## Commit Summary

feat(control-plane): device pairing + short-lived access tokens (DevHub stage-B item 5)

## Commit Body

Replaces the static AXI_MOBILE_OWNER_TOKEN env-var bearer check with a per-device pairing flow plus 1-hour HS256 access tokens. The bootstrap fallback is preserved when AXI_MOBILE_PAIRING_ENABLED is not 'true'.

services/control-plane/src/pairing.mjs (new, ~340 lines, 0 npm deps):
- createPairingService({ cacheDir, tokenSecret, codeTtlSeconds=300, tokenTtlSeconds=3600 }).
- startPair({ publicKeyHex, deviceName, clientInfo }) -> { pairingId, code, codeExpiresAt }; code is a 6-digit number stored in cacheDir/pairing-codes.jsonl.
- confirmPair({ pairingId, code }) -> { deviceId, status: 'active', nonce }; nonce is a single-use token the device signs with its private key.
- requestAuthNonce / verifyNonceSignature / issueAccessToken / verifyAccessToken / revokeDevice / listDevices / audit.
- HS256 sign/verify implemented in-tree using node:crypto.createHmac + base64url (RFC 7515 §3). Public-key fingerprint via HMAC-SHA256(publicKeyHex, 'axi-mobile-pubkey-fingerprint-v1'). Timing-safe equality for signature and code comparisons.
- Persistence layout: cacheDir/devices/<deviceId>.json (chmod 600), cacheDir/nonces/<nonceId>.json (chmod 600), cacheDir/pairing-codes.jsonl + access-tokens.jsonl (chmod 600 at first write). Existing cacheDir/audit.jsonl picks up new auditKind: 'mobile_pairing' entries.
- _internals exposed for tests only (signHs256, verifyHs256, base64url, fingerprintPublicKey, readDevice, writeDevice, devicesDir, pairCodesPath, tokensPath).

services/control-plane/src/control-plane.mjs:
- Imports createPairingService; reads AXI_MOBILE_TOKEN_SECRET and AXI_MOBILE_PAIRING_ENABLED env vars.
- pairing is null when AXI_MOBILE_TOKEN_SECRET is unset so existing tests/devs that never set the env keep working without ceremony.
- createControlPlane() now returns pairingEnabled (boolean) and pairing (service or null) so the server can route.

services/control-plane/src/server.mjs:
- /mobile/v1/* block now hosts 3 unauthenticated routes (pair/start, pair/confirm, auth/token), 1 token-gated nonce-refresh route (/auth/nonce), 1 token-gated revoke route (/pair/revoke), and the 5 existing token-gated routes.
- isPairedOwner() is narrowed to a bootstrap-only fallback: pairingRequired = pairingEnabled || Boolean(controlPlane.pairing). When pairing is required, the static token check is bypassed and only the device-token path is honoured.
- authenticate() is the new central gate; the 5 existing /jobs /:id/cancel /approvals/:id/decision routes now propagate deviceId into the control-plane payload so subsequent commits can record auditDeviceId without re-plumbing.
- /jobs/:id/cancel and /projects/:id (read) intentionally do NOT require deviceId at the control plane — they are pure reads on already-stored records; device identity for the audit trail will be threaded by the next commit.

services/control-plane/test/pairing.test.mjs (new, 12 cases):
- startPair code/id shape; non-hex publicKeyHex rejection; confirmPair nonce + deviceId; wrong-code rejection; expired-code rejection (clock override); second-use rejection.
- verifyNonceSignature + issueAccessToken HS256 round-trip; tampered signature rejection; revokeDevice -> verifyAccessToken fails.
- Expired token (clock override) and wrong-secret tokens both rejected.
- listDevices returns active + revoked records.

services/control-plane/test/mobile-routes.test.mjs (new, 2 cases):
- Full end-to-end: 401 without bearer -> pair/start -> pair/confirm -> issueAccessToken -> GET /mobile/v1/workspace returns the projected project.
- Revoked device's previously-issued token gets 401 on subsequent request.

Net: 19 -> 33 cases, all pass; npm dependency count unchanged.

Plan: /Users/mose/.claude/plans/devhub-mobile-stage-b.md §Owner decisions (fallback retained, token TTL 1h, code TTL 5 min, audit chmod 600) all reflected here.

Tested: pnpm --filter @axi/workstation-control-plane test -> 33/33 (was 19, +14).
Not-tested: end-to-end on a real Android client; the go cross-decoder parity check against services/auth-service/middleware/jwt.go (deferred to a follow-up commit if needed).
Confidence: high.
Scope-risk: narrow — only services/control-plane + its tests; no other service, package, or app touched.
Directive: After this commit, /mobile/v1/pair/* and /mobile/v1/auth/* exist for the Android client to consume. The static AXI_MOBILE_OWNER_TOKEN is still honoured only when AXI_MOBILE_PAIRING_ENABLED is not 'true', so bootstrap paths are unchanged. The next commit wires idempotencyKey + auditEvent into the three POST write paths; do not pile that into this commit.

## Changed Paths

- services/control-plane/src/control-plane.mjs
- services/control-plane/src/pairing.mjs
- services/control-plane/src/server.mjs
- services/control-plane/test/mobile-routes.test.mjs
- services/control-plane/test/pairing.test.mjs

## Verification

- Tested: pnpm --filter @axi/workstation-control-plane test -> 33/33 (was 19, +14).
- Not-tested: end-to-end on a real Android client; the go cross-decoder parity check against services/auth-service/middleware/jwt.go (deferred to a follow-up commit if needed).

## Remaining Risks

end-to-end on a real Android client; the go cross-decoder parity check against services/auth-service/middleware/jwt.go (deferred to a follow-up commit if needed).
