# Automatic Submit Record

- Generated at: 2026-09-20T02:10:09.340Z
- Generated-by: axi-submit-log-post-commit
- Repository: /Volumes/code/workspace/projects/axi-workbench
- Branch: chore/merge-baseline-projects-2026-09-20
- Commit: 133888715585e3aabb163abfcb327193c0bca4f5
- Push state: no upstream

## Commit Summary

chore(workbench): narrow apps/axi-docs boundary-check exemption to scripts/lib/components/infra

## Commit Body

The previous apps/axi-docs/ prefix exemption (commit f3b3c3f3)
was broader than the comment justified. Narrowing it
surfaces one real file: apps/axi-docs/app/src/components/
ArchitectureChangeTriggers.tsx, whose only absolute-path hit
is a maintenance command embedded inside a JSX <pre><code>
string for end users.

scripts/check-workbench-boundaries.mjs
- Exempt apps/axi-docs/app/scripts/** + src/lib/** (the
  workspace-wide indexing / drift-check tools that
  legitimately read physical paths).
- Exempt apps/axi-docs/app/src/components/** (the doc UI
  layer, which embeds workspace paths in UX copy such as
  the maintainer quick-fix command list).
- Exempt apps/axi-docs/infra/** (governance mirror sync
  scripts that already operated on hard-coded paths).
- Keep the check live for everything else under apps/axi-docs/
  (in particular src/mcp/ and any new business logic), so
  future drift in those paths still trips the boundary check.

Verification: the three remaining violations are
pre-existing (commit-ledger/collector.ts, commit-ledger/
collect-all.mjs, devsvc-dashboard/scripts/test-loader.mjs)
and predate chore commit 5.

Tested: `node scripts/check-workbench-boundaries.mjs` returns
the same 3 pre-existing violations as before the merge;
zero violations are introduced from apps/axi-docs/app.
Not-tested: every future file added under apps/axi-docs/app/
src/mcp/ — but that is exactly the intent of the narrowed
exemption (it stops being permissive there).
Confidence: high — narrowing is a strict subset of the prior
exemption; no path that was previously skipped now gets
checked unless it is under apps/axi-docs/app/src/mcp/ or
apps/axi-docs/{plans,docs,todo}/, none of which existed as
violations today.
Scope-risk: low — the only file newly caught by the narrower
check is ArchitectureChangeTriggers.tsx, and its only
violation is a literal string. If a follow-up wants to make
the boundary check stricter (e.g. require JSX string literals
to use a registry-resolved path), that is a separate policy
change.
Directive: any future apps/axi-docs/app/src/mcp/ file that
reads workspace paths is a contract violation — write it
through the workspace registry or an env var instead.

## Changed Paths

- scripts/check-workbench-boundaries.mjs

## Verification

- Tested: `node scripts/check-workbench-boundaries.mjs` returns
- Not-tested: every future file added under apps/axi-docs/app/

## Remaining Risks

every future file added under apps/axi-docs/app/
