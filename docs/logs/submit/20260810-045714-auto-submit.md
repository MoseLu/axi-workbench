# Automatic Submit Record

- Generated at: 2026-08-09T20:57:14.520Z
- Generated-by: axi-submit-log-post-commit
- Repository: /Volumes/code/workspace/projects/axi-workbench
- Branch: dev
- Commit: 050f12f0cc7bb1eb40ec16536fbadb8601e6da4f
- Push state: upstream=origin/dev; ahead=1; behind=0

## Commit Summary

fix(workbench): audit i18n dictionary coverage and clean dead keys

## Commit Body

A static analysis of the workbench i18n dictionaries surfaced two
classes of bugs:

1) Two keys were referenced by t() but missing from the dictionaries
   and silently fell back to the raw key string at runtime:
   - MainLayout.tsx called t('layout.floatingTools.search') for the
     global search trigger label, but the dictionary exposed only
     layout.floatingTools.{label,open,close,fallback,panelHint}.
     Add layout.globalSearch.label and rewire both call sites
     (the floating tool item label and the dashboard shell's
     globalSearchLabel prop).
   - Dashboard.tsx and MenuList.tsx called t('projects.column.index')
     for the table row-index column title, but the key was not in
     either dictionary. Add it.

2) Twelve keys were defined but unreferenced by any component after
   the recent migrations (login/register/password/email duplicates,
   app.brand, nav.settings / nav.settings.user, and the notification
   channel + status + description keys whose consumers live in
   getTaskStatusLabel / categoryLabel rather than in the dictionary).
   Drop them so the dictionaries stay in lock-step with the rendered
   chrome.

Restored auth.email because Login.tsx uses it for the email field
label; that consumer was previously migrated and the key is in
active use.

After cleanup: 373 keys in zh-CN.json, 373 keys in en-US.json, 0
keys referenced but undefined, 0 keys defined but unreferenced,
and no diff between the two dictionaries.

Tested:
- pnpm --filter @axi/workbench type-check
- pnpm --filter @axi/workbench test (19 files, 64 tests)
- pnpm --filter @axi/workbench build

Not-tested:
- Full cross-browser matrix, Android packaging, and production deployment

Confidence:
- High for both locales; the static key-vs-call-site analysis is the
  source of truth.

Scope-risk:
- Two locale dictionaries and one consumer file only.

Directive:
- Every key in both dictionaries must be referenced by at least one
  component, and every t() call site must resolve to a defined key.
  Add audit-friendly grep patterns to the team playbook so future
  migrations cannot silently regress coverage.

## Changed Paths

- apps/workbench/src/i18n/locales/en-US.json
- apps/workbench/src/i18n/locales/zh-CN.json
- apps/workbench/src/layouts/MainLayout.tsx

## Verification

- Tested: not recorded
- Not-tested: none

## Remaining Risks

none
