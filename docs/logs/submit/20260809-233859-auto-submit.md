# Automatic Submit Record

- Generated at: 2026-08-09T15:38:59.848Z
- Generated-by: axi-submit-log-post-commit
- Repository: /Volumes/code/workspace/projects/axi-workbench
- Branch: dev
- Commit: ee8d83dc8060f48a1ed491fde7e188e1838ed93a
- Push state: upstream=origin/dev; ahead=36; behind=0

## Commit Summary

fix(verification-inbox): wrap bare hex colors in design-token fallback

## Commit Body

verification-inbox had 14 bare hex literals in regular rule blocks
(green/blue/red/amber/gray status pill colors). Wrap each in
var(--axi-*, #hex) so the design-token theme layer can take over
once the app imports @axi/tokens/css; until then the fallback
preserves the current visual exactly.

- #15803d / #166534 / #047857 -> --axi-success
- #0369a1 / #1d4ed8           -> --axi-primary
- #475569                     -> --axi-text-secondary
- #b45309                     -> --axi-warning
- #dc2626                     -> --axi-danger

This app does not yet import @axi/tokens/css, so today the var()
always falls through to the literal; the wrap is a no-op for
rendering but removes the bare-hex audit hit and prepares the
color layer for a one-line token import later.

Tested:
- pnpm --filter axi-verification-inbox build
- git diff --cached --check
- git status --short --branch

Not-tested:
- In-browser visual diff (fallback makes behavior identical to
  before, so no diff is expected)
- Cross-browser matrix, Android packaging, production deploy

Confidence:
- High for build; high for visual no-op (fallback is the original
  hex) and audit cleanup

Scope-risk:
- apps/verification-inbox/src/styles/app.css only; other files in
  this app (base.css, layout.css) are untouched and also free of
  bare hex in regular rule blocks

Directive:
- When this app imports @axi/tokens/css, verify that the chosen
  --axi-success / -primary / -warning / -danger values match the
  current hex fallbacks; if a future token round changes the role
  values, the fallback chain keeps the visual stable.

## Changed Paths

- apps/verification-inbox/src/styles/app.css

## Verification

- Tested: not recorded
- Not-tested: none

## Remaining Risks

none
