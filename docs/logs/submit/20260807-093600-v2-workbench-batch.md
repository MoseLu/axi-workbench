# Submit Record — Axi Workbench v2 Workbench Batch (2026-08-07)

- Generated at: 2026-08-07T01:36:00.000Z
- Generated-by: git-commit-batch whole-repository diff cleanup
- Repository: /Volumes/code/workspace/projects/axi-workbench
- Branch: dev
- Push state: ahead=15; not pushed (batch scope = local cleanup only)

## Batch Scope

Convert the 1700+ dirty path whole-repository diff into 15 intentional
local commits. Each commit is feature-aligned and ships with the
required Lore trailers (Tested, Not-tested, Confidence, Scope-risk,
Directive). The batch preserves the documentation suite refresh
(`docs/state/PRD|TDD|TODO|MILESTONE|CHANGELOG|VERIFICATION.md`) and
the workbench rebrand (`apps/web-portal` → `apps/workbench`) while
moving legacy Vue/qiankun-era packages into a reference archive.

## Commit Roll-up

1. `ff23b10` — feat(archive): preserve legacy web/desktop packages under archive/
2. `4c1ae16` — chore(deps): refresh pnpm-lock.yaml after legacy package archive
3. `e8582c1` — feat(workbench): rebrand web-portal into single SPA workbench entrance
4. `c62efdf` — feat(workbench-foundation): add shared shell primitives package
5. `704d394` — feat(services): api-gateway + auth qrcode + notification channel expansion
6. `6a042dd` — fix(devsvc-dashboard): refine AlertsPage, ServersPage, ServicesPage wiring
7. `d9a9f56` — feat(axi-coder): drive workspace-project handoff via environment contracts
8. `2189215` — chore(packages-ui): tidy shell CSS variables and bump apps/AGENTS note
9. `dc17cc8` — docs(root): archive README + refresh README/HANDOFF/01-overview for v2 workbench
10. `820a16f` — fix(archive): complete packages/desktop removal that group 1 missed
11. `32d306c` — fix(archive): complete packages/web removal that group 1 missed
12. `b02d3f5` — chore(roots): bump @axi/workstation-contracts auth schema + root manifests
13. `157ee22` — docs(state): refresh PRD/TDD/TODO/MILESTONE/CHANGELOG + new VERIFICATION

Per-commit auto-submit records already exist under
`docs/logs/submit/20260807-*.md`; this roll-up gives the operator a
single landing page when reviewing the batch scope.

## Cross-Cutting Intent

- **Documentation suite v2**: PRD.md 升到 13 个 `REQ-*`，TDD.md 加上按
  surface 的验证命令矩阵 + 10 项 Risk Cases，TODO.md 每个 P0/P1 任务都
  锚定到 `REQ-*`，新增 VERIFICATION.md，CHANGELOG 与 MILESTONE 同步。
- **Workbench 入口收敛**: `apps/web-portal` 删除，`apps/workbench` 成为
  唯一 SPA 工作台入口，desktop ≥ 768px 用 Axi Dashboard Chrome，mobile
  < 768px 用独立移动端壳（对应 PRD REQ-WORKBENCH-001/002）。
- **六层控制面守门**: `services/api-gateway` + `services/auth-service`
  + `services/notification-service` 增强，`packages/schemas` 增加
  mobile device-nonce 合同；`pnpm check:boundaries` 在每次相关提交里
  留作 verification 一项。
- **Legacy 归档**: `packages/web` 与 `packages/desktop` 整目录移到
  `archive/legacy-packages-{web,desktop}/`，保留作历史参考，不污染
  active workspace graph。

## Out-of-Scope (intentionally left dirty)

- `.workbuddy/` 与 `.superpowers/` 是本地 harness 状态目录，按本仓库
  `AGENTS.md` 「不适用对象」段不进入 commit；本次也未修改 `.gitignore`，
  留待单独授权的治理更新处理。
- `docs/superpowers/` 里的 `plans/` 同样属 harness 状态，不进入 commit。

## Verification

- `pnpm type-check` 工作树状态在每个 commit 边界保持 green。
- `pnpm --filter @axi/workbench test` 23 个单元测试仍 passed（在
  Group 3 reb 提交时确认）。
- `pnpm --filter @axi/workstation-control-plane smoke` 维持 ≥ 35
  资源、六层快照（与最近 `e64dbc7` 一致）。
- 自动 `docs/logs/submit/20260807-*.md` 已生成；本 roll-up 是该批次的
  目录级别摘要。

## Next-Safe-Action

继续 devsvc-dashboard 的 hosted-mode 验证或 inbox triage 等下游工作；
本批已使工作树清洁（除开 harness 状态目录），不再有未提交的实现
改一动混在 docs commit 里。
