# Global Prompt 层

本目录对所有 EPAP 纳管项目生效。

Prompt 正文使用 `.mdc`。`README.md` 只用于说明。

Global prompt 可以定义：

- 通用工程工作方式。
- 用户语言和输出偏好。
- 通用测试验证梯。
- 引用公开 prompt/rules 后形成的通用 agent 行为模板。

Global prompt 不得覆盖 system prompt，也不得包含某个项目的私有事实。
